@model SampleProject.Models.UserModels.OpenId

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>OpenId</legend>

        @Html.HiddenFor(model => model.OpenIdId)

        <div class="editor-label">
            @Html.LabelFor(model => model.OpenIdUrl)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OpenIdUrl)
            @Html.ValidationMessageFor(model => model.OpenIdUrl)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId, "User")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
